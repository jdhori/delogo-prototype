# delogo local inpainting backend
# Python 3.10+ recommended.

fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9   # multipart/form-data parsing for the upload
pydantic>=2.6

# --- Remote video import (URL / YouTube / Vimeo / YuJa / Kaltura) ---
yt-dlp>=2025.1            # resolves + downloads a URL to a local MP4 (fetcher.py)

# --- AI chat integration (MCP) ---
mcp>=1.2                  # Model Context Protocol SDK (mcp_server.py)
websockets>=12            # editor page <-> MCP bridge

# --- Audio description (optional) ---
# TTS for `task: "describe"` jobs. Without it the server falls back to espeak-ng
# (system package) or an audible placeholder tone — see server/README.md.
# piper-tts>=1.2

# --- ProPainter / deep video inpainting (install when wiring the real processor) ---
# These are NOT needed to run the stub processor (ffmpeg delogo). Uncomment and
# install once you clone ProPainter and switch DELOGO_PROCESSOR=propainter.
#
# torch>=2.1            # install the CUDA build matching your driver (cu121 for 12GB cards)
# torchvision>=0.16
# opencv-python>=4.9
# numpy>=1.26
# scipy>=1.12
# imageio>=2.34
# imageio-ffmpeg>=0.4.9
# av>=12.0
