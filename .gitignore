# OS junk
.DS_Store
Thumbs.db

# Local-only test assets (not part of the prototype itself)
_sample.mp4
sample.mp4
_frame_*.png
*.mkv
*.mov

# Local AI / editor tooling (config + chat history live outside the repo)
.claude/
.cursor/
.aider*
.continue/
.specstory/

# Self-hosted FFmpeg-WASM core (~32 MB). Recreated by setup.sh from unpkg.
ffmpeg/

# Editor / IDE
.vscode/
.idea/
*.swp

# Build / scratch
node_modules/
dist/
.cache/
